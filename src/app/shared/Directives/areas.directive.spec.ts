import { AreasDirective } from './areas.directive';

describe('AreasDirective', () => {
  it('should create an instance', () => {
    const directive = new AreasDirective();
    expect(directive).toBeTruthy();
  });
});
